import { IonPage } from "@ionic/react";
import React from "react";

export const AddressesPage = () => {
	return (
		<IonPage>
			<div>ESTA ES LA PAGINA de direcciones</div>
		</IonPage>
	);
};

// export default React.memo(AddressesPage);
