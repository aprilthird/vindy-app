import { IonPage } from "@ionic/react";
import React from "react";

export const AddressPage = () => {
	return (
		<IonPage>
			<div>ESTA ES LA PAGINA de una direccion</div>
		</IonPage>
	);
};

// export default React.memo(AddressPage);
