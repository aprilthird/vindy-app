import { IonPage } from "@ionic/react";
import React from "react";

export const FavoritesPage = () => {
	return (
		<IonPage>
			<div>ESTA ES LA PAGINA de favoritos</div>
		</IonPage>
	);
};

// export default React.memo(FavoritesPage);
