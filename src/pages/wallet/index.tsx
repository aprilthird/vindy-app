import { IonPage } from "@ionic/react";
import React from "react";

export const WalletPage = () => {
	return (
		<IonPage>
			<div>ESTA ES LA PAGINA de wallet</div>
		</IonPage>
	);
};

// export default React.memo(WalletPage);
