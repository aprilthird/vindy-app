import { IonPage } from "@ionic/react";
import React from "react";

export const ResetPasswordPage = () => {
	return (
		<IonPage>
			<div>ESTE ES EL RESET PASSWORD PAGE</div>
		</IonPage>
	);
};

// export default React.memo(ResetPasswordPage);
