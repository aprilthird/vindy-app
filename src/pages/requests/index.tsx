import { IonPage } from "@ionic/react";
import React from "react";

export const RequestsPage = () => {
	return (
		<IonPage>
			<div>ESTA ES LA PAGINA de de todas las solicitudes</div>
		</IonPage>
	);
};

// export default React.memo(RequestsPage);
